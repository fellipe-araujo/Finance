import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },

  content: {
    flex: 1,
    width: '80%',
  },

  accountLogo: {
    marginTop: 20,
  },

  break: {
    height: 50,
  },
});

export default styles;

import React from 'react';
import { View, Text } from 'react-native';
import styles from './styles';

const Objective = () => (
  <View style={styles.container}>
    <Text>My Objectives</Text>
  </View>
);

export default Objective;

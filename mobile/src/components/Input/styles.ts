import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 50,
    backgroundColor: '#FFF',
    borderRadius: 8,
    marginTop: 10,
    borderWidth: 2,
    padding: 8,
    color: '#39393A'
  },
});

export default styles;
